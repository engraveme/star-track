[project]
name = "githubctl"
version = "0.1.6"
description = ""
authors = [{name = "Peng Xiao", email = "xiaoquwl@gmail.com"}]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "typer[all]>=0.9.0",
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
    "jmespath>=1.0.1",
]

[project.scripts]
githubctl = "githubctl.main:app"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
